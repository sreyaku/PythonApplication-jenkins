before_script:
  - apk add --update curl && rm -rf /var/cache/apk/*
stages:
  - openMr


openMr:
    before_script:
      - apk update && apk add git

    stage: openMr

    script:
        - echo 'hai............'
        - git version
        - ssh-add -K ~/.ssh/[ssh key]
        - git push -o ci-skip git@gitlab.com:myrepo18/SampleProject.git HEAD:master